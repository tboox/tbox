sudo: required
language: c

install:
  - git clone --branch=master https://github.com/waruqi/xmake.git waruqi/xmake
  - cd ./waruqi/xmake
  - ./install
  - cd -

script:
  - xmake f -c
  - xmake -r
  - xmake f -m debug
  - xmake -r
